#include "disk.h"
#include <stdio.h>

int main() {
  printf("%d\n", sizeof(struct metadata));
//  printf("make:  %d\n", make_fs("test_disk.fs"));
//  printf("mount: %d\n", mount_fs("test_disk.fs"));
//  printf("umount:%d\n", umount_fs("test_disk.fs"));

  return 0;
}
